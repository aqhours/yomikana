# 项目协作规则

## 歌曲添加

- 用户只提供歌名时，主动完成素材查找、版本核对、翻译、分词注音、导入和非 UI 检查，遵循 `docs/song-import.md`。
- 先查找项目目录和 `~/Downloads` 中对应歌曲的音频与歌词，文件名匹配需兼容 Unicode 规范化；不要要求用户重复提供已有素材或路径。
- 没有用户译文时自行翻译，不要求用户提供译文、分词或封面。没有歌词本时先使用核对后的歌词来源，不把歌词本作为必需材料。
- 对确实无法自动取得的素材或必须由用户完成的登录验证，说明具体缺项；保留已经完成的资料，补齐后继续。不得把试听片段当完整音频，或用臆造的时间轴冒充逐字歌词。

## 前端 UI 验收

- 一切涉及前端 UI 界面的验收，均交由用户人工完成。
- 禁止使用 Computer Use 进行前端 UI 验收，包括通过浏览器或原生应用进行页面查看、截图、点击、滚动、播放、交互及视觉效果检查。
- 不得改用其他浏览器自动化工具替代 Computer Use 执行 UI 验收。
- 不主动为 UI 编写单元测试。
- 可以执行构建、lint 和非 UI 的代码、数据检查；这些检查不代表 UI 验收通过。
- 完成前端修改后，向用户提供页面入口及必要的验收说明，由用户判断界面、交互和视觉效果是否符合要求。
